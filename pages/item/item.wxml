<!--pages/item/item.wxml-->
<view class="itemPage">
  <view wx:for="{{itemList}}" wx:key="{{index}}" class="itemList">
      <image src="{{item.src}}" class="itemImg"></image>
      <text class="itemText">{{item.itemTitle}}</text>
      <text class="jianjiaohao">></text>
  </view>
</view>
